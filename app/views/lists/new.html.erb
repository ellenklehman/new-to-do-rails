<% content_for(:title, "New list | To Do") %>

<h1>New list</h1>

<%= render('layouts/errors', :object => @list) %>

<form action="/lists" method="post">
  <%= render('form') %>
  <button>Create List</button>
</form>
